extends partials/layout.pug 

block content 
    h1 Home
    h5 Post
    p.font-monospace= message

    each post in posts
        h5=post.title 
        p=post.content.slice(0,100) +" ...." 
            a(href=`/posts/${post.title}`) Read More

        //- - console.log(post.title)
